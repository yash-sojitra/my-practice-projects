dictionary = {'mutable': 'can be changed',
              'immutable': 'cannot be changed',
              'set':'arrangement of well defined numbers',
              'bottle': 'object used to store and drink water' }

query = input('type the word to know its meaning \n ')
print(dictionary[query])
print(type(dictionary))