#include <stdio.h>
#include <string.h>

int main()
{
   int roll_no;
   scanf("%d", &roll_no);
   printf("%d", roll_no);

   char name[20];
   scanf("%s", &name);
   // gets(name);
   puts(name);
   return 0;
}